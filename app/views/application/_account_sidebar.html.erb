<div id="account-sidebar-wrap">
  <% if current_user.customer_id %>
    <h3>Welcome, <%= current_user.customer.name %>!</h2>
  <% else %>
    <h3>You are currently not linked to a company account.</h2>
    <%= link_to "Create new company account", new_customer_path %><br>
    <%= link_to "Existing company account" %>
  <% end %>
</div>